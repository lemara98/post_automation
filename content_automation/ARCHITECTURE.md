# System Architecture

## 📐 High-Level Overview

```
┌─────────────────────────────────────────────────────────────────────┐
│                        CONTENT AUTOMATION SYSTEM                     │
│                         (Betania.io)                                │
└─────────────────────────────────────────────────────────────────────┘

┌──────────────────┐
│   RSS FEEDS      │
│                  │
│ • .NET Blogs     │
│ • Python.org     │
│ • AI News        │───┐
│ • TechCrunch     │   │
│ • HackerNews     │   │
│ • GitHub         │   │
│ • StackOverflow  │   │
└──────────────────┘   │
                       │
                       ▼
         ┌─────────────────────────┐
         │  Azure Function (Daily) │
         │    Runs: 9:00 AM        │
         └─────────────────────────┘
                       │
            ┌──────────┴──────────┐
            ▼                     ▼
   ┌────────────────┐    ┌────────────────┐
   │ RSS Aggregator │    │  Filter Already │
   │  (Python)      │───▶│   Published     │
   └────────────────┘    └────────────────┘
                                  │
                                  ▼
                         ┌────────────────┐
                         │ AI Generator   │
                         │  (GPT-4)       │
                         │                │
                         │ • Blog Post    │
                         │ • LinkedIn Post│
                         └────────────────┘
                                  │
                    ┌─────────────┴─────────────┐
                    ▼                           ▼
          ┌──────────────────┐       ┌──────────────────┐
          │ WordPress API    │       │   Database       │
          │                  │       │   (MySQL)        │
          │ • Create Post    │       │                  │
          │ • Publish        │       │ • Track Articles │
          │ • Add Tags       │       │ • LinkedIn Queue │
          └──────────────────┘       └──────────────────┘
                    │
                    ▼
          ┌──────────────────┐
          │  Betania.io      │
          │  (Live Blog)     │
          └──────────────────┘
```

## 📅 Weekly Newsletter Flow

```
         ┌──────────────────────────┐
         │ Azure Function (Weekly)  │
         │   Runs: Sunday 10:00 AM  │
         └──────────────────────────┘
                      │
                      ▼
         ┌──────────────────────────┐
         │  Query Database          │
         │  (Past 7 days articles)  │
         └──────────────────────────┘
                      │
                      ▼
         ┌──────────────────────────┐
         │  AI Ranking              │
         │  Select Top 5 Articles   │
         └──────────────────────────┘
                      │
                      ▼
         ┌──────────────────────────┐
         │  Generate Newsletter     │
         │  • Intro                 │
         │  • Article summaries     │
         │  • HTML template         │
         └──────────────────────────┘
                      │
                      ▼
         ┌──────────────────────────┐
         │  Get Active Subscribers  │
         │  (Confirmed + Active)    │
         └──────────────────────────┘
                      │
                      ▼
         ┌──────────────────────────┐
         │  SendGrid Email Service  │
         │  Send to all subscribers │
         └──────────────────────────┘
                      │
                      ▼
         ┌──────────────────────────┐
         │  Subscriber Inboxes      │
         │  📧 Top 5 News           │
         └──────────────────────────┘
```

## 👥 Subscription Flow

```
┌──────────────────┐
│  WordPress Site  │
│  (betania.io)    │
└──────────────────┘
         │
         │ User visits page with
         │ [betania_newsletter_form]
         │
         ▼
┌──────────────────┐
│ Subscription     │
│ Form (Plugin)    │
│                  │
│ • Name input     │
│ • Email input    │
│ • Submit button  │
└──────────────────┘
         │
         │ User submits
         │
         ▼
┌──────────────────┐
│  AJAX Handler    │
│  (WordPress)     │
│                  │
│ • Validate email │
│ • Generate tokens│
└──────────────────┘
         │
         ▼
┌──────────────────┐
│  MySQL Database  │
│                  │
│  INSERT INTO     │
│  newsletter_     │
│  subscribers     │
└──────────────────┘
         │
         │ confirmed = FALSE
         │
         ▼
┌──────────────────┐
│ Confirmation     │
│ Email Service    │
│ (SendGrid/Python)│
└──────────────────┘
         │
         ▼
┌──────────────────┐
│ User Email Inbox │
│ "Confirm your    │
│  subscription"   │
└──────────────────┘
         │
         │ User clicks confirm link
         │
         ▼
┌──────────────────┐
│ WordPress Handler│
│ ?confirm=1&token │
│                  │
│ UPDATE confirmed │
│ = TRUE           │
└──────────────────┘
         │
         ▼
┌──────────────────┐
│ ✅ SUBSCRIBED    │
│ Will receive     │
│ weekly newsletter│
└──────────────────┘
```

## 🗄️ Database Schema

```sql
┌─────────────────────────────────────────────────────────────┐
│                  newsletter_subscribers                     │
├─────────────────────────────────────────────────────────────┤
│ id (PK)                  INT AUTO_INCREMENT                 │
│ email                    VARCHAR(255) UNIQUE NOT NULL       │
│ name                     VARCHAR(255)                       │
│ subscribed_at            DATETIME DEFAULT NOW()             │
│ confirmed                BOOLEAN DEFAULT FALSE              │
│ confirmation_token       VARCHAR(64)                        │
│ unsubscribe_token        VARCHAR(64) UNIQUE                 │
│ active                   BOOLEAN DEFAULT TRUE               │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│                    published_articles                       │
├─────────────────────────────────────────────────────────────┤
│ id (PK)                  INT AUTO_INCREMENT                 │
│ title                    VARCHAR(500) NOT NULL              │
│ source_url               VARCHAR(1000) UNIQUE               │
│ wordpress_post_id        INT                                │
│ wordpress_url            VARCHAR(1000)                      │
│ published_at             DATETIME DEFAULT NOW()             │
│ source_name              VARCHAR(255)                       │
│ tags                     TEXT                               │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│                      linkedin_queue                         │
├─────────────────────────────────────────────────────────────┤
│ id (PK)                  INT AUTO_INCREMENT                 │
│ article_id (FK)          INT                                │
│ post_content             TEXT NOT NULL                      │
│ created_at               DATETIME DEFAULT NOW()             │
│ posted                   BOOLEAN DEFAULT FALSE              │
│ posted_at                DATETIME                           │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│                     newsletter_sends                        │
├─────────────────────────────────────────────────────────────┤
│ id (PK)                  INT AUTO_INCREMENT                 │
│ sent_at                  DATETIME DEFAULT NOW()             │
│ subject                  VARCHAR(500)                       │
│ article_ids              TEXT                               │
│ recipient_count          INT                                │
└─────────────────────────────────────────────────────────────┘
```

## 🔄 Data Flow

### Daily Content Generation

```
1. Trigger (9:00 AM Daily)
   ↓
2. RSSAggregator.fetch_articles()
   → Returns: List[Article]
   ↓
3. ArticleModel.is_article_published()
   → Filters out duplicates
   ↓
4. AIContentGenerator.generate_blog_post()
   → Input: Article
   → Output: {title, content, excerpt, tags}
   ↓
5. WordPressClient.create_post()
   → Publishes to betania.io
   → Returns: {id, url}
   ↓
6. ArticleModel.add_article()
   → Saves to database
   ↓
7. AIContentGenerator.generate_linkedin_post()
   → Creates LinkedIn content
   ↓
8. LinkedInQueueModel.add_to_queue()
   → Stores for manual posting
```

### Weekly Newsletter

```
1. Trigger (Sunday 10:00 AM)
   ↓
2. ArticleModel.get_recent_articles(days=7)
   → Returns: List[Article] from DB
   ↓
3. AIContentGenerator.rank_articles_for_newsletter()
   → AI selects top 5 most valuable
   ↓
4. AIContentGenerator.generate_newsletter_intro()
   → Creates personalized intro
   ↓
5. SubscriberModel.get_active_subscribers()
   → Returns: List[Subscriber] (confirmed=true, active=true)
   ↓
6. EmailService.send_newsletter()
   → For each subscriber:
      → Generate HTML email
      → Add unsubscribe link
      → Send via SendGrid
   ↓
7. Track in newsletter_sends table
```

## 🧩 Component Dependencies

```
┌─────────────────────────────────────────────────────────┐
│                      config.py                          │
│  • Environment variables                                │
│  • RSS feed URLs                                        │
│  • API keys                                             │
└─────────────────────────────────────────────────────────┘
                          │
         ┌────────────────┼────────────────┐
         │                │                │
         ▼                ▼                ▼
┌──────────────┐  ┌──────────────┐  ┌──────────────┐
│ RSS          │  │ AI Content   │  │ WordPress    │
│ Aggregator   │  │ Generator    │  │ Client       │
└──────────────┘  └──────────────┘  └──────────────┘
         │                │                │
         └────────────────┼────────────────┘
                          │
                          ▼
               ┌──────────────────┐
               │  Database Models │
               │  • Subscribers   │
               │  • Articles      │
               │  • LinkedIn Queue│
               └──────────────────┘
                          │
         ┌────────────────┼────────────────┐
         │                │                │
         ▼                ▼                ▼
┌──────────────┐  ┌──────────────┐  ┌──────────────┐
│ Daily Content│  │ Weekly       │  │ Email        │
│ Function     │  │ Newsletter   │  │ Service      │
│              │  │ Function     │  │              │
└──────────────┘  └──────────────┘  └──────────────┘
```

## 🚀 Deployment Architecture

```
┌─────────────────────────────────────────────────────────┐
│                    AZURE CLOUD                          │
│                                                         │
│  ┌──────────────────────────────────────────────────┐  │
│  │         Azure Functions (Consumption Plan)        │  │
│  │                                                   │  │
│  │  ┌─────────────────┐  ┌─────────────────┐       │  │
│  │  │ daily_content   │  │weekly_newsletter│       │  │
│  │  │ Timer: 9:00 AM  │  │Timer: Sun 10:00 │       │  │
│  │  └─────────────────┘  └─────────────────┘       │  │
│  │                                                   │  │
│  │  Environment Variables (Azure Key Vault)         │  │
│  │  • OPENAI_API_KEY                                │  │
│  │  • WORDPRESS_USERNAME                            │  │
│  │  • SENDGRID_API_KEY                              │  │
│  │  • Database credentials                          │  │
│  └──────────────────────────────────────────────────┘  │
│                                                         │
│  ┌──────────────────────────────────────────────────┐  │
│  │         Application Insights (Monitoring)         │  │
│  │  • Logs                                           │  │
│  │  • Metrics                                        │  │
│  │  • Alerts                                         │  │
│  └──────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────┘
                          │
                          │ HTTPS
                          │
         ┌────────────────┼────────────────┐
         │                │                │
         ▼                ▼                ▼
┌──────────────┐  ┌──────────────┐  ┌──────────────┐
│ WordPress    │  │ OpenAI API   │  │ SendGrid     │
│ betania.io   │  │              │  │              │
│              │  │              │  │              │
│ MySQL DB     │  │              │  │              │
└──────────────┘  └──────────────┘  └──────────────┘
```

## 💰 Cost Structure

### Monthly Costs (Estimated)

```
┌─────────────────────────────────────────────────┐
│  Service              │ Usage      │ Cost       │
├─────────────────────────────────────────────────┤
│  Azure Functions      │ ~1K runs   │ $0 (free)  │
│  OpenAI GPT-4         │ ~150 calls │ $15-25     │
│  SendGrid             │ ~400 emails│ $0 (free)  │
│  WordPress Hosting    │ Existing   │ (existing) │
│  MySQL Database       │ Existing   │ (existing) │
├─────────────────────────────────────────────────┤
│  TOTAL                │            │ $15-25/mo  │
└─────────────────────────────────────────────────┘

Scaling to 1000+ subscribers:
• Azure Functions: Still free tier
• SendGrid: Still free (100/day = 400/week for newsletter)
• OpenAI: $20-40/month
• Total: $20-40/month
```

## 🔐 Security Architecture

```
┌─────────────────────────────────────────────────┐
│            Security Layers                      │
├─────────────────────────────────────────────────┤
│  1. Environment Variables (.env)                │
│     • Never committed to git                    │
│     • Local development only                    │
├─────────────────────────────────────────────────┤
│  2. Azure Key Vault (Production)                │
│     • Encrypted secrets storage                 │
│     • Managed identities                        │
├─────────────────────────────────────────────────┤
│  3. WordPress Application Passwords             │
│     • Separate from admin password              │
│     • Revokable                                 │
│     • Scoped permissions                        │
├─────────────────────────────────────────────────┤
│  4. Database Security                           │
│     • Parameterized queries (SQL injection)     │
│     • Input validation                          │
│     • Email verification (double opt-in)        │
├─────────────────────────────────────────────────┤
│  5. API Rate Limiting                           │
│     • MAX_ARTICLES_PER_DAY limit                │
│     • OpenAI usage monitoring                   │
├─────────────────────────────────────────────────┤
│  6. Unsubscribe Security                        │
│     • Unique tokens per subscriber              │
│     • No account access required                │
└─────────────────────────────────────────────────┘
```

## 📈 Scalability

### Current Capacity

- **Articles**: 5 per day = 1,825 per year
- **Newsletter**: 1 per week to unlimited subscribers
- **Database**: Handles 100K+ subscribers easily
- **Azure Functions**: 1M executions/month (free tier)

### Scaling Path

```
Phase 1 (Current):
• 5 articles/day
• 100 subscribers
• Cost: $15-25/month

Phase 2 (Growth):
• 10 articles/day
• 1,000 subscribers
• Add content categories
• Cost: $30-50/month

Phase 3 (Scale):
• 20 articles/day
• 10,000+ subscribers
• Multi-language support
• Dedicated database
• Cost: $100-200/month
```

---

**Architecture Version**: 1.0
**Last Updated**: 2025-11-30
